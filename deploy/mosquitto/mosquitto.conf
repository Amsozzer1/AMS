# Mosquitto config for the AMS-X bus (local-first scaffold).
listener 1883
listener 9001
protocol websockets

persistence true
persistence_location /mosquitto/data/

# Scaffold default: anonymous on the LAN. BEFORE exposing anything beyond the LAN,
# switch to authenticated access (password_file / per-client ACLs).
allow_anonymous true

log_dest stdout
